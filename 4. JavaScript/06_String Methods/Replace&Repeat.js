// Replace Method searches a value in the string and returns a new string with the value replaced.
let str = "IamMonkeyDLuffy"

let newStr = str.replace("Monkey", "Dragon")
console.log(newStr)

// Repeat Method returns a string with the number of copies of a string.
let str2 = "Namisan"

console.log(str2.repeat(3))
// Trim it and convert it to uppercase
let msg = "  help!  "
console.log(msg.trim().toUpperCase())

// For the str predict the output.
let str = "OnePiece"
console.log(str.slice(2, 5))
console.log(str.indexOf("Pi"))
console.log(str.replace("Piece", "King"))
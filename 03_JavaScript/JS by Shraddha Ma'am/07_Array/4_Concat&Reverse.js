let primary = ["red", "yellow", "blue"]
let secondary = ["orange", "green", "violet"]

// Concatenation - merge 2 arrays
console.log(primary.concat(secondary))  //it doesn't change original array

// Reverse - reverse an array
console.log(primary.reverse()); //it changes original array also in reverse
console.log(secondary.reverse());